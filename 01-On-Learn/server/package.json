{
  "name": "server",
  "version": "1.0.0",
  "description": "A backend for online learning application where you can host, sell, buy and watch your courses",
  "main": "server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=local nodemon src/server",
    "start": "cross-env NODE_ENV=production node src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.4",
    "nodemailer": "^7.0.5",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
